[homeserver]
# Run createuser_playbook using this.
192.168.1.10 ansible_connection=ssh ansible_user="root" ansible_password="{{ proxmox_password }}"

# Run basic_setup_playbook as the new user
# 192.168.1.10 ansible_connection=ssh ansible_user="{{ proxmox_username }}" ansible_password="{{ proxmox_password }}"\

# Run everything using this
# 192.168.1.10 ansible_connection=ssh ansible_user="{{ proxmox_username }}" ansible_ssh_private_key_file="{{ ssh_key_path }}" ansible_ssh_port="{{ ssh_port }}"
